version: '3.4'

networks:
  topcoder-dev:
    driver: bridge 

services:
  postgres:
    image: postgres:latest
    ports:
      - "5432"
    restart: always
    volumes:
      - db_volume:/var/lib/postgresql/data
      - ./dbscripts/seed.sql :/docker-entrypoint-initdb.d/seed.sql
    environment:
      POSTGRES_USER: "bloguser"
      POSTGRES_PASSWORD: "bloguser"
      POSTGRES_DB: "blogdb"
    networks:
      - topcoder-dev
  app:
    depends_on:
      - "postgres"
    build: .
    ports:
      - "5000:80"
    environment:
      PostgresDB__DATABASE: blogdb
      PostgresDB__Host: postgres
      PostgresDB__PORT: 5432
      PostgresDB__USER: bloguser
      PostgresDB__PASSWORD: bloguser
    networks:
      - topcoder-dev  
volumes:
  db_volume:
         
    
     